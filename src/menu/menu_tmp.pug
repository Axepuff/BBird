ul.menu
  each item in items
    li.menu__item
      a(class = "menu__link" href = item.link data-action="pick")= item.name
      button(class = "menu__close close" type = "button" aria-label="Close" data-action='remove')
